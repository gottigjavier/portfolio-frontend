import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SpokenLangEditComponent } from './spoken-lang-edit.component';

/* describe('SpokenLanguagesEditComponent', () => {
  let component: SpokenLangEditComponent;
  let fixture: ComponentFixture<SpokenLangEditComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ SpokenLangEditComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(SpokenLangEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */